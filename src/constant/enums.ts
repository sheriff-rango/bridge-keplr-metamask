export enum WalletType {
  "METAMASK",
  "KEPLR",
}

export enum WalletConnectStatus {
  "WAITING_CONNECT",
  "BEFORE_CONNECT",
  "CONNECTED",
}

export enum ConfirmStatus {
  "WAITING_CONFIRM",
  "BEFORE_CONFIRM",
  "CONFIRMED",
}
